## v0.3.1 (2025-09-10)

### 変更概要
翻訳照合タブの表記調整と、EN/JAディレクトリ未指定時の自動補完の挙動を改善しました。

### 変更点
- UI: 翻訳照合タブのタイトルから「BG3」を削除。
- Matcher: 実行時に EN/JA ディレクトリが未入力の場合でも自動補完されるように修正。
  - `base_dir` が指定されていれば相対で補完。
  - `base_dir` 未指定時は `data\\bundles\\bg3_official` を基準として `English` / `Japanese` を既定値に設定。

### 互換性
- 破壊的変更はありません（後方互換）。

### アップグレード手順
- 特別な手順は不要です。通常どおりアプリを再起動してください。

### 既知の注意点
- なし。

### 関連
- ブランチ: `release/v0.3.1`
- タグ: `v0.3.1`


